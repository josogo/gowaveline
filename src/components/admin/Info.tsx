
import { AlertTriangle, Info } from 'lucide-react';

// This component is just to satisfy the Info import in CommissionTracking.tsx
export default function InfoComponent() {
  return (
    <Info className="h-5 w-5" />
  );
}
